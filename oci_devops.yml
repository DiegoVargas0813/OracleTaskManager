version: 0.1
component: build
timeoutInSeconds: 1200
shell: bash

env:
  variables:
    JAVA_HOME: /usr/lib64/graalvm/graalvm22-ee-java17
    DOCKER_REGISTRY: mx-queretaro-1.ocir.io/axvz6pcfltnr/hellodevops/deploy1
    OCIR_USERNAME: axvz6pcfltnr/a01635782@tec.mx
    OCIR_AUTH_TOKEN: mgLChj]GCkxLACp]rt5P

steps:
  - type: Command
    name: "Check Directory Structure"
    command: |
      echo "Current directory: $(pwd)"
      echo "Directory contents:"
      ls -R

  - type: Command
    name: "Docker Login to OCIR"
    command: |
      echo "$OCIR_AUTH_TOKEN" | docker login -u "$OCIR_USERNAME" --password-stdin mx-queretaro-1.ocir.io


  - type: Command
    name: "Run Build in Oracle Linux 8 Container"
    command: |
      echo "📦 Mounting workspace: $(pwd)"
      docker run --rm \
        -v $PWD:/workspace \
        -v /var/run/docker.sock:/var/run/docker.sock \
        -w /workspace/MtdrSpring/backend \
        oraclelinux:8 \
        bash -c "
          set -e

          echo '🧰 Installing build dependencies...'
          yum -y install oracle-epel-release-el8 && \
          yum -y install nodejs npm maven java-17-openjdk-devel docker && \
          echo '✅ Dependencies installed.'

          echo '📁 Checking contents of /workspace:'
          ls -l /workspace
          echo '📁 Checking contents of /workspace/MtdrSpring:'
          ls -l /workspace/MtdrSpring
          echo '📁 Checking contents of /workspace/MtdrSpring/backend:'
          ls -l /workspace/MtdrSpring/backend

          echo '📜 Sourcing environment variables...'
          if [ ! -f /workspace/MtdrSpring/env.sh ]; then echo '❌ env.sh not found'; exit 1; fi
          source /workspace/MtdrSpring/env.sh

          echo '🚀 Running build script...'
          ./build.sh
        "

  - type: Command
    name: "Check JAR Exists After Build"
    command: |
      echo "Verifying JAR output..."
      if [ ! -f MtdrSpring/backend/target/MyTodoList-0.0.1-SNAPSHOT.jar ]; then
        echo "ERROR: JAR file not found!"
        ls -l MtdrSpring/backend/target
        exit 1
      else
        echo "JAR file found:"
        ls -lh MtdrSpring/backend/target/*.jar
      fi
